# GitHub Stars Crawler

This project crawls GitHub repositories using the GraphQL API and stores star counts in a PostgreSQL database.

## Folder Structure
- `crawl_stars.py` – main crawler script
- `sql/setup.sql` – database schema
- `.github/workflows/crawl.yml` – GitHub Actions workflow
